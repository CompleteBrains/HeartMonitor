<Window x:Class="Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:charts="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
        Width="800" Height="800">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="Style">
                <Setter Property="Control.HorizontalAlignment" Value="Left" />
                <Setter Property="Control.FontSize" Value="26" />
                <Setter Property="Control.Margin" Value="20"></Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel>
        <Button Name="Send" Content="Send" Style="{StaticResource Style}" />
        <TextBlock Name="Received" Style="{StaticResource Style}"/>
        <charts:Chart Name="Data" MinHeight="500">
            <charts:LineSeries ItemsSource="{Binding Data}" DependentValuePath="Value" IndependentValuePath="Key"/>
        </charts:Chart> 
    </StackPanel>
</Window>